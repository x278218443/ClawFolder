## 抖音自动发布修复（续）

### 问题
- cron 环境没有 nvm/node PATH → npm 找不到
- 封面裁切弹窗 (ReactCrop) 挡住标题输入和描述输入
- daemon 需要 xvfb 才能在 cron 环境启动 headful 浏览器

### 修复
1. `auto_news_video.sh` — 开头加载 nvm PATH
2. `douyin_publish_cli.py` — get_ws_url 改用 daemon `/browser/acquire` 接口；用 xvfb-run 启动 daemon
3. `douyin_publish.py` — 裁切弹窗用 JS `document.querySelectorAll('.semi-portal').forEach(el => el.remove())` 强制移除
4. 不再关闭现有页面（会导致 daemon 退出）

### 验证
- 2026-05-16 AI 早报手动补发抖音成功（共 3 个作品）
- B站自动发布成功：BV1pF5r6sEKx

### 待观察
- 明天 5:00 cron 是否全自动跑通
- daemon 稳定性（xvfb 模式）
